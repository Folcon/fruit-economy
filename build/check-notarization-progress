#!/bin/bash

echo "Env Var:"
echo "NOTARY_TOKEN: $NOTARY_TOKEN"

n=0
until [ "$n" -ge 5 ]
do
  NOTARY_CHECK_OUTPUT=$(/usr/bin/xcrun altool --notarization-info "$NOTARY_TOKEN" --username $NOTARY_USERNAME --password $NOTARY_PASSWORD --output=json) && break
  n=$((n+1))
  sleep 60
done
